/**
 * @module domains
 * @summary Central export point for all domain modules
 * @description
 * This file will export all domain modules as they are implemented.
 * Each domain should have its own directory with a _module.ts file.
 */

// Domain exports will be added here as features are implemented
// Example: export * from './expense';
// Example: export * from './income';
