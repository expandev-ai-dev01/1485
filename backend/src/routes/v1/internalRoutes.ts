import { Router } from 'express';

const router = Router();

/**
 * @summary Internal API routes configuration
 * @remarks Feature-specific routes will be added here as features are implemented
 * @example router.use('/expense', expenseRoutes);
 * @example router.use('/income', incomeRoutes);
 */

export default router;
